<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:vm="clr-namespace:Transfermarkt.MobileApp.ViewModels"
             mc:Ignorable="d"
             x:Class="Transfermarkt.MobileApp.ViewModels.LoginPage">
    <ContentPage.BindingContext>
        <vm:LoginViewModel></vm:LoginViewModel>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        
        <StackLayout HorizontalOptions="FillAndExpand" Padding="10" VerticalOptions="CenterAndExpand">
            <Label  Text="Username"></Label>
            <Entry  Text="{Binding Username}"></Entry>

            <Label  Text="Password"></Label>
            <Entry  Text="{Binding Password}" IsPassword="True"></Entry>

            <Button Command="{Binding LoginCommand}"  HorizontalOptions="FillAndExpand" Text="Login"></Button>
            <Label IsVisible="{Binding IsBusy}" Text="..."></Label>

            <Label Text="If you dont't have an account click the button below." HorizontalOptions="FillAndExpand"></Label>
            <Button Command="{Binding RegisterCommand}" Text="Sign up!"></Button>
        </StackLayout>
        
    </ContentPage.Content>
</ContentPage>