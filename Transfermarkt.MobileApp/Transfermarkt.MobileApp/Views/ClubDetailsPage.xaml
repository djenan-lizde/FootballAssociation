<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:vm="clr-namespace:Transfermarkt.MobileApp.ViewModels" xmlns:converters="clr-namespace:Transfermarkt.MobileApp.Converters"
             mc:Ignorable="d"
             x:Class="Transfermarkt.MobileApp.Views.ClubDetailsPage">
    <ContentPage.BindingContext>
        <vm:ClubDetailsViewModel></vm:ClubDetailsViewModel>
    </ContentPage.BindingContext>
    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:ImageConverter x:Key="imgConv"></converters:ImageConverter>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Club!"
                VerticalOptions="Center" 
                HorizontalOptions="Center" />
            <StackLayout Grid.Row="0" Grid.Column="0">
                <!--<StackLayout>
                    <Image Source="{Binding Club.Logo, Converter={StaticResource imgConv}}"></Image>
                </StackLayout>-->
                <Label Text="{Binding Club.Name}"></Label>
                <Label Text="{Binding Club.Nickname}"></Label>
                <Label Text="{Binding Club.MarketValue}"></Label>
            </StackLayout>
            <ListView ItemsSource="{Binding Players}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Padding="10" Orientation="Horizontal">
                                <StackLayout Orientation="Horizontal">
                                    <Label Text="{Binding FirstName}"></Label>
                                    <Label Text="{Binding LastName}"></Label>
                                    <Label Text="{Binding Jersey}"></Label>
                                    <Label Text="{Binding Birthdate}" LineBreakMode="NoWrap"></Label>
                                </StackLayout>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>